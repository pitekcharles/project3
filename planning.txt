data structure

user table
    id = key, unique
    username = unique string
    password    

Character table
    id = key, unique
    character name
    server name
    class
    user id
    attendence on-time score
    attendence late score
    attendence absent score
    attendence leave early score

guild table
    id = key unique
    guild name
    server name
    guild master id


switch to buttons to adjust the attendence score, one button adds, one button subtracts. component map to populate the page, already in place easy
take old attendence score and push it to the db, remove that person from the state to reload section (added bonus) also add a button to populate attendence so 
it does not have issues/ it is not always there